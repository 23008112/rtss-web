# Public base URL of the deployed site. Used for the sitemap, robots.txt,
# and to resolve absolute Open Graph / Twitter image URLs.
# Set this to the real production domain before deploying.
NEXT_PUBLIC_SITE_URL=https://rtss-web.vercel.app
